{
  "name": "mongoose-multiple-connections",
  "version": "1.0.0",
  "description": "Manage multiple Mongoose connections in a simple way",
  "main": "build/index.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "batto",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/node12": "^1.0.7",
    "@types/mongoose": "^5.10.1",
    "@types/node": "^14.14.10",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "mongoose": "^5.11.1"
  }
}
